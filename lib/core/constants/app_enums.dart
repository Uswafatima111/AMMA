enum UserRole {
  asha,
  patient,
}

enum RiskLevel {
  low,
  moderate,
  high,
}

enum VisitType {
  routine,
  followUp,
  emergency,
}

enum SyncStatus {
  pending,
  syncing,
  synced,
  failed,
}

enum AlertSeverity {
  low,
  moderate,
  high,
}

enum ReferralStatus {
  pending,
  accepted,
  completed,
  cancelled,
}